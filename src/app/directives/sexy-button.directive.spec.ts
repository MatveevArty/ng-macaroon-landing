import { SexyButtonDirective } from './sexy-button.directive';

describe('SexyButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SexyButtonDirective();
    expect(directive).toBeTruthy();
  });
});
